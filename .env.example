# Copiaza acest fisier in .env si completeaza valorile tale
# Prioritate: variabile de mediu > .env > application.properties

DB_URL=jdbc:postgresql://localhost:5433/prognoza_meteo
DB_USER=postgres
DB_PASSWORD=postgres

DB_POOL_MAX_SIZE=10

# Daca folosesti Docker, portul implicit e 5433:
# DB_URL=jdbc:postgresql://localhost:5433/prognoza_meteo
